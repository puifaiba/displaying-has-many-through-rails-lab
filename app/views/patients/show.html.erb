<h1>Appointments</h1>
<p><% @patient.appointments.each do |a| %>
    <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
    <%= link_to a.doctor.name, doctor_path(a.doctor) %><br>
    <% end %>
</p>