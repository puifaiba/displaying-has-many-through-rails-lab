<h3><%= @doctor.name %></h3>
<h3><%= @doctor.department %></h3>
<p><% @doctor.appointments.each do |a| %>
    <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
    <%= link_to a.patient.name, patient_path %><br>
    <% end %>
</p>